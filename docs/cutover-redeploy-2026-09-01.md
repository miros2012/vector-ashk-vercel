# Controlled Decision Engine cutover redeploy

This documentation-only commit triggers the single production redeploy required to load `DECISION_STATE_WRITES_ENABLED=true` into the Vercel runtime. No runtime code changes are included.
